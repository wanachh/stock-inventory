{
  "name": "stock-inventory",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "dotnet run --project backend/StockInventory.Api.csproj",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "export": "npm --prefix frontend run build"
  }
}
