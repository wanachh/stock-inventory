{
  "name": "stock-inventory",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && rm -rf out && cp -r frontend/out out",
    "export": "npm --prefix frontend run build"
  }
}
